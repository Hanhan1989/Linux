version: "3"
services:

    ubuntu:
        container_name: mi_ubuntu
        build:
            context: .
        volumes:
            - ../:/practicas
        privileged: true
        stdin_open: true
        tty: true